// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/python
{
	"name": "ib-env",
	// Or use a Dockerfile or Docker Compose file. More info: https://containers.dev/guide/dockerfile
	// "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bullseye"
	"build": {
		"dockerfile": "../DOCKERFILE"
		},

	"mounts": [
        "source=${localEnv:USERPROFILE}/Documents/academic/data/Glaze_Triangles,target=/workspaces/behavioral_data_reanalyses_IB/data/Glaze_Triangles,type=bind,consistency=cached",
		"source=${localEnv:USERPROFILE}/Documents/academic/data/auditory_triangles,target=/workspaces/behavioral_data_reanalyses_IB/data/auditory_triangles,type=bind,consistency=cached"
    ]

	// "mounts": [
	// 	"source=${localEnv:HOME}/Documents/academic/data/Murphy2021,target=${containerWorkspaceFolder}/data/Murphy2021,type=bind,consistency=cached"
	// ]

	// added to solve bug
	// "initializeCommand" : "ls"

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],

	// Use 'postCreateCommand' to run commands after the container is created.
	// "postCreateCommand": "pip3 install --user -r requirements.txt",

	// Configure tool-specific properties.
	// "customizations": {},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	// "remoteUser": "root"
}
